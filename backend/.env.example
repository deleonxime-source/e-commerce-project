PORT=4001
DATABASE_URL=postgresql://user:pass@host:5432/db

# Asgardeo JWT (org slug must match frontend baseUrl: .../t/<ASGARDEO_ORG>)
ASGARDEO_ORG=yourorg
# Optional overrides (defaults: JWKS = .../t/${ASGARDEO_ORG}/oauth2/jwks, iss = .../t/${ASGARDEO_ORG}, aud = SPA client id)
# If JWT verify still fails, paste your access token at jwt.io and set ASGARDEO_AUDIENCE to the exact `aud` (comma-separate if multiple)
# ASGARDEO_JWKS_URI=
# ASGARDEO_ISSUER=
# ASGARDEO_AUDIENCE=
